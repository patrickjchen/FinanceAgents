# Base URL of the FinanceAgents backend the chat page posts to (POST {API_URL}/query).
# All four implementations (langchain / crewai / llamaindex / ag2) listen on 8000; run one at a time.
NEXT_PUBLIC_API_URL=http://localhost:8000
